# @package _global_

model_name: ???
completion_type: chat
routing_strategy: least-busy
redis_host: ${oc.env:REDIS_HOST}
redis_port: ${oc.env:REDIS_PORT}
redis_client_kwargs:
  ssl: True
  ssl_check_hostname: False
allowed_failures: 10
cooldown_time: 120
retry_after: 5
num_retries: 4
seed: ${..seed}
temperature: 0.7
max_tokens: 2048
