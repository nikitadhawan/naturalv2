# @package _global_

model_name: ???
completion_type: chat
routing_strategy: least-busy
allowed_failures: 10
cooldown_time: 2
retry_after: 5
num_retries: 4
seed: ${..seed}
temperature: 0.7
max_tokens: 512
thinking:
  type: disabled
  budget_tokens: 0
